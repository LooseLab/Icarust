name: icarust
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      grpcio==1.43.0
      grpcio-tools==1.43.0
      h5py==3.6.0
      numpy==1.22.2
      pyfastx
      rich
      git+https://github.com/alexomics/scrappie#subdirectory=python