name: icarust
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python==3.9
  - pip
  - pip:
      - grpcio
      - grpcio-tools
      - h5py==3.6.0
      - numpy==1.22.2
      - mappy
      - rich
      - git+https://github.com/alexomics/scrappie#subdirectory=python
      - pandas